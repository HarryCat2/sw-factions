package harry.cat.sw_factions.util;


import net.minecraft.core.BlockPos;
import net.minecraft.server.level.ServerLevel;
import net.minecraft.world.level.chunk.ChunkAccess;

// Given a position, which chunk is it in?

public class ChunkProtectionUtil {

//    private static boolean isWithinChunkProtection(ServerLevel world, ChunkAccess chunk, BlockPos pos) {
//        int chunkX = pos.getX() / 16;
//        int chunkZ = pos.getZ() / 16;
//
//        ChunkKey key = ChunkKey.of(world, chunkX, chunkZ);
//
//        return ProtectionManager.isProtected(key);
//    }
}
